spring:
  cloud:
    aws:
      region:
        static: "us-east-1"
      credentials:
        access-key: "test"
        secret-key: "test"
      s3:
        endpoint: "http://s3.localhost.localstack.cloud:4566"
      dynamodb:
        endpoint: "http://localhost:4566"

testdata:
  location: src/test/resources/data
  tableNames: pets, single_table, specialities, types, vet_specialities, vets, visits